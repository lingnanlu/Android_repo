<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.lingnanlu.gaoxiaolian">

    <!-- 基础模块（必须加入以下声明）START -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 基础模块 END -->
    <application
        android:name=".GaoXiaoLian"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.LoginActivity"
            android:label="@string/login"
            >

        </activity>

        <activity
            android:name=".ui.activity.HomeActivity"
            android:label="@string/home"
            ></activity>

        <activity

            android:name=".ui.activity.RegisterActivity"
            android:label="@string/register"
            android:parentActivityName=".ui.activity.LoginActivity"

            >
        </activity>


        <activity

            android:name=".ui.activity.UserInfoActivity"
            android:label="@string/personal_info"

            >

        </activity>

        <activity
            android:name=".ui.activity.EditActivity"
            android:label="@string/edit"
            android:parentActivityName=".ui.activity.HomeActivity"
            ></activity>

        <activity
            android:name=".ui.activity.FFActivity"
            android:label="@string/follower_followee"
            android:parentActivityName=".ui.activity.HomeActivity"
            ></activity>

        <activity
            android:name=".ui.activity.OnlineActivity"
            android:label="@string/online"
            android:parentActivityName=".ui.activity.HomeActivity"
            ></activity>


        <activity android:name=".ui.activity.ConversationsActivity"></activity>
        <activity android:name=".ui.activity.ChatActivity"></activity>
        <activity android:name=".ui.activity.MatchResultActivity"></activity>
        <activity android:name=".ui.activity.MatchActivity"></activity>
        <activity android:name=".ui.activity.MatchSettingActivity"></activity>

        <!-- 实时通信模块、推送（若使用该功能，需添加以下声明）START -->
        <service android:name="com.avos.avoscloud.PushService" />

        <receiver android:name="com.avos.avoscloud.AVBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
        <!-- 实时通信模块、推送 END -->
        <activity android:name=".ui.activity.BaseActivity"></activity>
    </application>

</manifest>